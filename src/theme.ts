// Placeholder for theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00695c'
    },
    secondary: {
      main: '#26a69a'
    },
    background: {
      default: '#e0f2f1',
      paper: '#ffffff'
    }
  }
});

export default theme;
